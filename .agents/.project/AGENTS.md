# Umbrella workspace guidance

This repository is a gitlink inventory. Preserve child working trees and make pointer-only updates unless the active issue explicitly scopes child-repository work.

Load `PROJECT.md`, `COMMANDS.md`, and `STATUS.md` before changing the inventory. Validate the exact changed gitlinks and do not initialize or rewrite unrelated archived or parked repositories.
